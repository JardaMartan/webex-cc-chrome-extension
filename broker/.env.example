# Example environment for broker/server.js — copy to `.env` (and load with
# `node --env-file=.env server.js` on Node 20.6+, or export these yourself)
# and NEVER commit the real values.

WEBEX_CLIENT_ID=your-integration-client-id
WEBEX_CLIENT_SECRET=your-integration-client-secret
ALLOWED_ORIGIN=chrome-extension://olckgckmelfihnkbibckgllpegijjeek
PORT=8787
